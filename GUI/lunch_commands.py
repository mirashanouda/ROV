
import subprocess

#command = subprocess.run("ls -a", shell=True) #, , stdout=subprocess.PIPE, stderr=subprocess.STDOUT , capture_output=True
#subprocess.run("bash roscore.sh", shell=True)
#print(command.stdout.decode())